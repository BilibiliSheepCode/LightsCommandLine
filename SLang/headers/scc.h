#ifndef _SCC_H_
#define _SCC_H_
#include<stdio.h>
#include<stdlib.h>
#include<conio.h>
typedef long long int Int;
typedef char Char;
typedef double Float;
typedef enum _Status {ERROR=-1,SUCCESS=0,OVERFLOW=1} Status;
#endif